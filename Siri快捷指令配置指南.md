# iPhone Siri 快捷指令配置指南

## 概述

通过 Siri 快捷指令，可以在不打开 PWA 控制台的情况下，用语音完成入库、出库、查库存操作。所有指令最终通过调用后端 HTTP API 实现数据写入飞书多维表格。

---

## 前置条件

1. PWA 系统已部署并可 HTTPS 访问（`https://your-domain.com:your-port`）
2. iPhone 已安装「快捷指令」App（系统自带）
3. iPhone 与 NAS 在同一网络或 NAS 有公网访问

---

## 快捷指令一：语音入库

### 功能
用语音说出物料信息，自动识别并写入飞书多维表格，支持 6 大品类。

### 配置步骤

1. 打开 iPhone「快捷指令」App
2. 点击右上角「+」新建快捷指令
3. 点击顶部标题，重命名为「物料入库」
4. 点击「添加操作」，搜索「听写文本」
5. 添加「听写文本」操作，语言设为「中文（中国大陆）」
6. 再添加「获取URL内容」操作：

```
方法: POST
URL: https://your-domain.com:your-port/api/inbound/voice
请求体: JSON
添加新字段:
  键: text
  值: 听写的文本（选择上一步的变量）
  键: source
  值: siri
```

7. 添加「显示结果」操作，显示「获取URL内容」的结果

### 语音指令示例

对着 Siri 说「物料入库」，然后说出以下格式的信息：

> "PLA线材 规格1.75毫米 颜色黑色 数量5卷 放在A3货架"

> "沉头螺丝 M3x10 数量200个 存放在B2收纳盒"

> "黄铜喷嘴 0.4毫米 数量3个 品牌拓竹"

### API 接口说明

```
POST /api/inbound/voice
Content-Type: application/json

{
  "text": "用户语音转文字后的内容",
  "source": "siri"
}

返回:
{
  "success": true,
  "draft": {
    "material_name": "PLA线材",
    "category": "3D打印耗材",
    "spec": "1.75mm",
    "color": "黑色",
    "quantity": 5,
    "location": "A3货架"
  },
  "message": "AI已识别，请在PWA中确认后入库"
}
```

> **重要**：Siri 语音入库后，AI 识别结果为草稿状态，必须打开 PWA 控制台人工确认后才能正式写入飞书多维表格。这是系统强制安全机制。

---

## 快捷指令二：语音查库存

### 功能
用语音查询特定物料库存状态。

### 配置步骤

1. 新建快捷指令，命名为「查库存」
2. 添加「听写文本」操作
3. 添加「获取URL内容」操作：

```
方法: GET
URL: https://your-domain.com:your-port/api/inventory/search?keyword=听写的文本
```

4. 添加「显示结果」操作

### 语音指令示例

> "查一下PLA线材还剩多少"
> "黑色PETG库存"
> "M3螺丝还有多少"

系统会返回匹配的物料列表及当前库存数量。

---

## 快捷指令三：快速出库

### 功能
对着 Siri 说出出库指令，标记物料已用完（3D耗材）或扣减数量（其他品类）。

### 配置步骤

1. 新建快捷指令，命名为「物料出库」
2. 添加「听写文本」操作
3. 添加「获取URL内容」操作：

```
方法: POST
URL: https://your-domain.com:your-port/api/outbound/voice
请求体: JSON
添加新字段:
  键: text
  值: 听写的文本
  键: source
  值: siri
```

4. 添加「显示结果」操作

### 语音指令示例

> "白色PLA线材用完了"
> "M3x10沉头螺丝 消耗了50个"
> "黄铜喷嘴还剩2个"

---

## 添加快捷指令到主屏幕（可选）

1. 在快捷指令详情页，点击底部「分享」图标
2. 选择「添加到主屏幕」
3. 可自定义图标和名称
4. 点击「添加」

这样就可以像 App 一样一键触发语音录入。

---

## 触发方式

- **语音触发**：长按电源键唤醒 Siri，说快捷指令名称（如「物料入库」）
- **手动触发**：打开快捷指令 App，点击对应卡片
- **主屏幕触发**：点击添加到桌面的快捷指令图标

---

## 注意事项

1. **网络要求**：iPhone 必须能访问 NAS 的 HTTPS 地址（同一局域网或公网）
2. **HTTPS 证书**：首次访问可能有证书警告，需在 Safari 中先手动打开一次 `https://your-domain.com:your-port` 并信任证书
3. **AI 确认机制**：所有通过 Siri 的入库/出库操作，AI 识别结果均为草稿，必须在 PWA 中人工确认后才生效
4. **语音清晰度**：尽量在安静环境使用，语速适中，逐项说清楚物料名称、规格、数量
5. **快捷指令权限**：首次使用时会请求「听写」和「网络访问」权限，请全部允许

---

## 扩展：iOS 18+ 集成（可选）

iOS 18 支持更自然的 Siri 交互：

- 可在快捷指令详情中开启「通过 Siri 请求」开关
- 设置 → Siri → 快捷指令 → 添加快捷指令
- 之后可以直接说「嘿 Siri，物料入库」触发
